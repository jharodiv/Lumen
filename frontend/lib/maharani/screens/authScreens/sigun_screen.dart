import 'package:flutter/material.dart';

class SigunScreen extends StatelessWidget {
  const SigunScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}